{
  "name": "renderer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "cross-env BASEDIR=./ vite build",
    "build:github": "cross-env BASEDIR=/serial-bowl/ vite build",
    "build:localhost": "cross-env BASEDIR=/ vite build",
    "preview": "vite preview",
    "deploy": "npm run build:github && gh-pages -d ../dist/renderer",
    "serve": "npm run build:localhost && npx serve ../dist/renderer"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^4.2.0",
    "cross-env": "^10.0.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.2.0",
    "vite-plugin-pwa": "^1.0.2"
  },
  "homepage": "https://dragazzo01.github.io/serial-bowl/"
}
