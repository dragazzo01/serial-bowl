{
  "name": "serial-bowl",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/main/main.js",
  "author": "Daniel Ragazzo",
  "description": "Keeps track of your serialized stories from manga to manhwa to webnovels",
  "scripts": {
    "dev": "concurrently -k \"npm run dev:main\" \"npm run dev:renderer\"",
    "dev:main": "npm run build:main && wait-on tcp:5173 && electron .",
    "dev:renderer": "cd renderer && npm run dev",
    "build": "npm run build:renderer && npm run build:main",
    "build:renderer": "cd renderer && npm run build",
    "build:main": "tsc -p main/tsconfig.json",
    "watch:main": "tsc -w -p main/tsconfig.json",
    "clean": "rimraf dist release",
    "serve": "cd renderer && npm run serve",
    "netlify": "netlify dev",
    "prod": "npm run clean && npm run build && npx electron-builder --linux snap && sudo snap install ./release/serial-bowl_1.0.0_amd64.snap --dangerous",
    "test": "cd main && tsc -p tsconfig.json && npx ts-node test.ts"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "firebase": "^12.12.1",
    "puppeteer": "^24.39.1"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.35",
    "@types/node": "^20.0.0",
    "@types/puppeteer": "^5.4.7",
    "@types/react": "^19.1.10",
    "concurrently": "^8.2.2",
    "electron": "^41.0.2",
    "electron-builder": "^26.0.12",
    "playwright": "^1.60.0",
    "typescript": "^5.0.0",
    "wait-on": "^7.0.1"
  },
  "build": {
    "appId": "com.yourcompany.yourapp",
    "productName": "SerialBowl",
    "files": [
      "dist/**/*",
      "node_modules/**/*",
      "public/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": "nsis",
      "icon": "public/icon.ico"
    },
    "linux": {
      "icon": "public/icon.png"
    }
  }
}
